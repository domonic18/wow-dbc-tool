Metadata-Version: 2.4
Name: wow-dbc-tool
Version: 0.1.0
Summary: 魔兽世界 3.3.5 DBC 文件操作工具（面向 Agent）
License: MIT
Project-URL: Homepage, https://github.com/deadwalk/wow-dbc-tool
Classifier: Development Status :: 3 - Alpha
Classifier: Intended Audience :: Developers
Classifier: License :: OSI Approved :: MIT License
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.9
Classifier: Programming Language :: Python :: 3.10
Classifier: Programming Language :: Python :: 3.11
Classifier: Programming Language :: Python :: 3.12
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Provides-Extra: dev
Requires-Dist: pytest>=7.0; extra == "dev"
Requires-Dist: pytest-cov>=4.0; extra == "dev"
Requires-Dist: mypy>=1.0; extra == "dev"
Requires-Dist: black>=23.0; extra == "dev"
Requires-Dist: ruff>=0.1.0; extra == "dev"

# wow-dbc-tool

魔兽世界 3.3.5 DBC 文件操作工具（面向 Agent）

## 功能
- 读取 DBC 文件（WDBC 格式）
- 增删改查记录（CRUD）
- 查询操作符（eq/ne/gt/lt/contains）
- 对比两个 DBC 文件版本差异（Diff）
- Schema 字段定义管理（内置 + 自定义）
- JSON 结构化输出

## 安装

```bash
git clone <repo>
cd wow-dbc-tool
pip install -e .
```

## CLI 使用

```bash
# 读取 DBC 文件
wow-dbc-tool read Spell.dbc --json

# 查询记录
wow-dbc-tool query Spell.dbc --filter ID=133 --json
wow-dbc-tool query Spell.dbc --filter "Name__contains=Fire" --json

# 修改记录
wow-dbc-tool edit Spell.dbc --filter ID=133 --set Name="New Spell" --output Spell_new.dbc

# 删除记录
wow-dbc-tool delete Spell.dbc --filter ID=133 --output Spell_new.dbc --json

# 添加记录
wow-dbc-tool add Spell.dbc --field ID=9999 --field Name="Custom" --output Spell_new.dbc --json

# 对比两个 DBC 文件
wow-dbc-tool diff Spell_old.dbc Spell_new.dbc --json

# Schema 管理
wow-dbc-tool schema list --json
wow-dbc-tool schema show Spell.dbc --json
wow-dbc-tool schema infer Spell.dbc --output schema.json
```

## Python API

```python
from wow_dbc_tool import DBCFile

# 加载
dbc = DBCFile('Spell.dbc').load()

# 查询
records = dbc.query(Name__contains="Fire")

# 修改
for r in records:
    r.set('Name', r.get('Name') + ' (Modified)')

# 保存
dbc.save('Spell_modified.dbc')

# Diff
from wow_dbc_tool.diff import DBCDiff
report = DBCDiff(dbc_old, dbc_new).compare()
print(report.to_json())
```

## 项目结构

```
wow-dbc-tool/
├── pyproject.toml
├── README.md
├── docs/
│   └── architecture.md
├── src/wow_dbc_tool/
│   ├── __init__.py
│   ├── __main__.py
│   ├── cli.py
│   ├── commands/
│   ├── core/
│   ├── parser/
│   ├── schema/
│   └── diff/
├── schemas/
└── tests/
```
